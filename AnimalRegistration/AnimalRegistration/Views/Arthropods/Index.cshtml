@model IEnumerable<AnimalRegistration.Arthropod>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.arthropodsName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.arthropodsType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DietryType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PopulationNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.animal.animalID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.arthropodsName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.arthropodsType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DietryType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PopulationNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.arthropodsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.arthropodsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.arthropodsID })
        </td>
    </tr>
}

</table>
